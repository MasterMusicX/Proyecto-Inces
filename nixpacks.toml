[phases.setup]
nixPkgs = ["...", "php82Extensions.pdo_pgsql", "php82Extensions.pgsql"]

[phases.release]
cmds = ["php artisan optimize:clear", "php artisan migrate --force", "php artisan db:seed --force"]